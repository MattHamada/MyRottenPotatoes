%h2 Details about #{@movie.title}
%ul#details
  %li
    Rating:
    = @movie.rating
  %li
    Released on:
    = @movie.release_date.strftime("%B %d, %Y")
%ul Description:
%p#description= @movie.description
= link_to 'Edit movie info', edit_movie_path
%br
-# delete link is really a form acting like a link
= button_to 'Delete Movie', movie_path(@movie), :method => :delete, :confirm => 'Are you sure?'
%br
= link_to 'Back to movie list', movies_path